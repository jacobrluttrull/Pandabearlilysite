# OS
.DS_Store
Thumbs.db

# IDE
.idea/

# Claude Code agents / skills (local tooling config, not site content)
.agents/
.claude/
skills-lock.json

# Node / SvelteKit (web/)
node_modules/
.svelte-kit/
build/
dist/
.output/
.vercel/

# Go (soundboard-api/)
*.exe
*.exe~
*.test
*.out

# SQLite
*.db
*.sqlite
*.sqlite3

# soundboard-api local runtime state — the SQLite db only. Clip audio lives in
# soundboard-api/clips/ and IS committed: it ships in the Docker image, and it is
# the only backup of the audio that exists.
soundboard-api/data/

# raw clip staging folder — working copies, not the served collection
/Pandalily Soundbites/

# Environment
.env
.env.*
!.env.example

# Logs
*.log


#Designs
/designs

# personal notes / drafts, not part of the site
/info

go.work
go.work.sum

# Playwright MCP scratch output (screenshots/snapshots taken during review)
/.review
/.playwright-mcp
